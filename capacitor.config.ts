import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ankitsharma.workouttracker',
  appName: 'workout-tracker',
  webDir: 'dist'
};

export default config;
